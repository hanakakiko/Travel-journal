# 配置模板功能说明

## 功能概述

现在你可以保存和应用配置模板，大大提高工作效率！一次配置好所有选项后，下次只需上传照片，然后应用模板即可快速填充所有配置，无需逐个选择。

## 使用流程

### 1. 保存模板

1. 上传照片后，点击「开始画手帐」按钮打开配置面板
2. 在「配置模板」部分，点击「💾 保存当前配置」按钮
3. 输入模板名称（例如：「旅游风格」、「日常记录」等）
4. 点击确认，模板即被保存

### 2. 应用模板

1. 上传新的照片后，打开配置面板
2. 在「配置模板」部分，点击「📋 应用模板 (N)」按钮展开已保存的模板列表
3. 找到想要应用的模板，点击「应用」按钮
4. 所有配置（场景、情绪、叙述方式、视觉风味、风格、模板等）会立即填充
5. 你仍然可以修改任何选项，然后点击「装订手帐本」生成

### 3. 管理模板

- **查看模板**：点击「📋 应用模板」按钮可以看到所有已保存的模板及其创建日期
- **删除模板**：在模板列表中点击「删除」按钮可以删除不需要的模板
- **修改配置**：应用模板后，你可以修改任何选项，然后保存为新的模板

## 保存位置

所有模板都保存在浏览器的本地存储中，不会上传到服务器。这意味着：

- ✅ 模板数据完全私密，只在你的浏览器中保存
- ✅ 清除浏览器缓存前，模板会一直保留
- ⚠️ 清除浏览器缓存或使用无痕模式时，模板会丢失

## 模板包含的配置项

每个模板会保存以下配置：

- **生成模型**：选择的 AI 模型（FLUX.2、GPT-2 等）
- **场景**：一次旅程、日常生活等
- **场景细节**：根据场景的补充信息（目的地、同行人、天气等）
- **情绪**：怀旧、像电影等（可多选）
- **叙述方式**：像一本精致生活杂志等
- **视觉风味**：氛围标签、照片形状、边缘风格、装饰元素、纸张纹理等
- **风格**：优雅、复古、旅行等
- **排版模板**：图集、拼贴、杂志、档案等

## 使用建议

1. **为不同场景创建模板**：
   - 「旅游记录」模板：旅游场景 + 旅行风格
   - 「日常生活」模板：日常生活场景 + 柔和风格
   - 「美食分享」模板：美食相关配置

2. **定期整理**：删除不常用的模板，保持列表清爽

3. **快速迭代**：应用模板后，可以微调配置，然后保存为新模板

## 技术细节

- 模板数据使用 JSON 格式存储在 `localStorage` 中
- 存储键为 `journal-templates`
- 每个模板包含唯一 ID、名称、创建时间和完整配置
- 模板大小通常在 1-5KB 之间，不会占用太多存储空间

## 常见问题

**Q: 模板会不会丢失？**
A: 只要不清除浏览器缓存，模板就会一直保留。建议定期备份重要的配置。

**Q: 可以导出模板吗？**
A: 目前不支持导出，但可以通过浏览器开发者工具的 Application > Local Storage 查看和复制模板数据。

**Q: 模板数量有限制吗？**
A: 理论上没有限制，但浏览器 localStorage 通常有 5-10MB 的限制。正常使用不会达到这个限制。

**Q: 应用模板后还能修改吗？**
A: 完全可以！应用模板只是快速填充配置，你仍然可以修改任何选项。
