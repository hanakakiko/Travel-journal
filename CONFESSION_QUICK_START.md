# 倾诉记录功能 - 快速开始指南

## 功能简介

在生成手帐前，用户可以输入"今天的倾诉"——记录今天的感想、心情、发生的事情。这段话可以：

1. **仅作为个人记录**（默认）
2. **融入画面生成**（可选）- 帮助 AI 更好地理解情绪
3. **在最终页面显示和复制**

## 使用步骤

### 第 1 步：选择所有选项
- 选择场景、情绪、叙述方式等

### 第 2 步：输入倾诉记录（新增）
在"今天的倾诉"部分输入文字：
```
比如：今天天气很好，和朋友去了海边，虽然有点累但很开心。
希望能把这份美好记录下来…
```

### 第 3 步：选择是否融入画面（可选）
勾选"将这段话作为风格指导和关键词提取，融入画面生成"：
- ✅ 勾选：AI 会根据你的情绪生成更贴切的画面
- ❌ 不勾选：仅作为个人记录，不影响生成

### 第 4 步：生成手帐
点击"装订手帐本"按钮

### 第 5 步：查看和复制
在最终页面，你会看到"💭 今天的倾诉"部分：
- 显示你输入的完整文字
- 点击"复制"按钮可一键复制到剪贴板

## 关键特性

| 特性 | 说明 |
|------|------|
| **完全可选** | 可以跳过不填 |
| **隐私保护** | 仅在本地存储，除非选择融入生成 |
| **灵活使用** | 可作为个人日记或生成指导 |
| **易于分享** | 一键复制到剪贴板 |
| **情绪感知** | AI 可根据情绪调整画面风格 |

## 示例场景

### 场景 1：仅作为个人记录
```
输入：今天在咖啡馆工作了一整天，完成了重要项目。
融入生成：❌ 不勾选
结果：倾诉记录仅显示在最终页面，不影响画面生成
```

### 场景 2：融入画面生成
```
输入：今天心情很好，天气晴朗，和家人一起去公园散步。
融入生成：✅ 勾选
结果：AI 会根据"心情好、晴朗、家人、散步"等关键词
      生成更温暖、明亮、温馨的画面
```

### 场景 3：跳过倾诉记录
```
不输入任何内容
结果：正常生成，最终页面不显示倾诉记录部分
```

## 技术细节

### 数据存储
```typescript
// 在 UserAnswers 中
confessionText?: string;           // 倾诉文字
includeConfessionInImage?: boolean; // 是否融入生成
```

### Prompt 集成
如果用户勾选了"融入生成"，倾诉记录会被添加到 LLM prompt 中：
```
用户的倾诉记录（作为情绪和风格指导）：「[用户输入的文字]」。
这段话反映了用户当下的心情和想法，请在生成画面时融入这种
情绪氛围和关键词提示，但不要直接引用或写出这段话的内容。
```

### 复制功能
- 使用现代 Clipboard API
- 自动降级到 `execCommand` 方案（旧浏览器）
- 复制成功后显示"已复制 ✓"反馈

## 常见问题

**Q: 如果我输入了倾诉记录但没有勾选"融入生成"，会怎样？**
A: 倾诉记录仅作为个人记录保存，不会影响 AI 生成的画面。但最终页面仍会显示这段话。

**Q: 倾诉记录有长度限制吗？**
A: 没有硬性限制，但建议不超过 500 字以获得最佳效果。

**Q: 倾诉记录会被保存吗？**
A: 仅在当前会话中保存。刷新页面后会重置。如果需要保存，可以使用复制功能。

**Q: 如果我选择融入生成，AI 会直接写出我的倾诉内容吗？**
A: 不会。AI 会根据倾诉内容的情绪和关键词来调整风格，但不会直接引用或写出原文。

**Q: 复制功能在所有浏览器都能用吗？**
A: 是的。现代浏览器使用 Clipboard API，旧浏览器自动降级到 `execCommand` 方案。

## 相关文件

- 功能说明：[CONFESSION_FEATURE.md](./CONFESSION_FEATURE.md)
- 类型定义：[src/types.ts](./src/types.ts)
- 主要实现：[src/App.tsx](./src/App.tsx)
- 样式文件：[src/styles.css](./src/styles.css)
- Prompt 集成：[src/lib/modelClient.ts](./src/lib/modelClient.ts)
