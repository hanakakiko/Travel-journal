# 自定义选项功能 - 快速开始指南

## 🎯 30 秒快速了解

现在所有表单字段都可以添加自定义选项了！

### 什么是自定义选项？

比如在「朋友聚会」场景中的「菜系/酒水」字段：

**默认选项**: 中餐、日料、西餐、甜品、酒水、奶茶/咖啡、BBQ/烧烤

**你可以添加**: 素食、清汤火锅、小龙虾、精酿啤酒 等等...

### 它会自动保存吗？

✅ 是的！同时保存到：
- 💾 本地浏览器缓存（立即生效）
- ☁️ 云端服务器（自动同步）
- 📱 其他设备（登录相同账户后自动同步）

---

## 👀 看一眼就懂

### 添加新选项

```
┌─────────────────────────────────────────┐
│ 朋友聚会                                 │
├─────────────────────────────────────────┤
│ 菜系/酒水 (多选)                        │
│                                         │
│ [中餐] [日料] [素食❌] [火锅❌] [➕添加] │
│  ○      ○      ✓       ○              │
│                                         │
│ (点击 [➕添加] 输入新菜系)              │
└─────────────────────────────────────────┘
```

### 自定义选项特征

- 🟠 **橙色背景** - 一眼识别自定义选项
- ❌ **删除按钮** - 可随时删除
- 📍 **紧邻预设** - 自定义选项排在预设后面
- 💾 **自动保存** - 无需手动保存

---

## 🚀 使用流程

### 步骤 1: 找到字段并点击添加

```
在任何场景的 chip 字段中找到 [➕添加] 按钮
```

### 步骤 2: 输入新选项

```
┌──────────────────────────┐
│ [输入新菜系____] [✓] [❌] │
└──────────────────────────┘

输入后可以：
• 按 Enter 确认
• 点击 [✓] 确认
• 点击 [❌] 取消
```

### 步骤 3: 自动保存

```
输入完成后立即：
✓ 显示在列表中
✓ 保存到本地
✓ 同步到云端（后台）
```

---

## 💡 常见场景示例

### 场景 1️⃣: 「朋友聚会 - 菜系」

```
默认选项: 中餐、日料、西餐、甜品、酒水、奶茶/咖啡、BBQ/烧烤

你想添加:
  [➕添加] → "素食" → ✓
  [➕添加] → "小龙虾" → ✓
  [➕添加] → "精酿啤酒" → ✓

现在列表变成:
中餐、日料、西餐、甜品、酒水、奶茶/咖啡、BBQ/烧烤、
素食❌、小龙虾❌、精酿啤酒❌
```

### 场景 2️⃣: 「周末日常 - 做了什么」

```
默认选项: 睡到自然醒、做饭、阅读、做手工、看电影、运动、整理房间、见朋友

你想添加:
  [➕添加] → "遛狗" → ✓
  [➕添加] → "健身" → ✓

现在可以选:
[睡到自然醒] [做饭] [阅读] ... [遛狗❌] [健身❌]
```

### 场景 3️⃣: 「城市散步 - 停留过的小店」

```
默认选项: 咖啡馆、买手店、独立书店、市集/摊位、甜品店、酒馆、公园/草坪

你想添加:
  [➕添加] → "创意园区" → ✓
  [➕添加] → "美术馆" → ✓

现在可以多选:
[咖啡馆] [买手店] ... [创意园区❌] [美术馆❌]
                    ✓        ○
```

---

## 📲 跨设备使用

### 同步流程

```
手机端:
  [➕添加] "素食" → 保存
                 ↓
         本地 localStorage
                 ↓
         CloudBase 云端
                 ↓
电脑端:
  打开应用（同账户）
         ↓
  自动加载云端设置
         ↓
  "素食" 自动出现✓
```

### 何时同步

| 操作 | 本地 | 云端 | 同步时间 |
|------|------|------|---------|
| 添加选项 | 立即 | 异步 | <1ms + 200-500ms |
| 删除选项 | 立即 | 异步 | <1ms + 200-500ms |
| 网络断开 | ✓ 保留 | ⏸ 等待 | 网络恢复后 |
| 换设备登录 | - | ✓ 加载 | 打开应用时 |

---

## ⚡ 快速参考

### 键盘快捷键

```
输入新选项时:
┌─────────────────┬────────────────┐
│ Enter           │ 确认并保存     │
│ Escape          │ 取消输入       │
└─────────────────┴────────────────┘
```

### 按钮说明

| 按钮 | 作用 | 位置 |
|------|------|------|
| ➕ | 打开输入框 | 选项列表末尾 |
| ✓ | 确认新选项 | 输入框右侧 |
| ❌ | 删除自定义选项 | 自定义选项右侧 |
| ❌ | 取消输入 | 输入框右侧 |

---

## ✅ 常见问题速答

### Q1: 自定义选项会丢失吗？
**A**: 不会！我们用了双重保护：
- 💾 本地缓存（浏览器关闭后保留）
- ☁️ 云端备份（网络故障也保留）

### Q2: 其他设备能看到我的自定义选项吗？
**A**: 能的！只要：
1. 用相同账户登录
2. 网络连接正常
3. 稍等云端同步完成（通常 <1 秒）

### Q3: 可以删除自定义选项吗？
**A**: 可以，点击自定义选项右侧的 ❌ 就行。

### Q4: 默认选项能删除吗？
**A**: 不能，默认选项是受保护的。

### Q5: 有字数限制吗？
**A**: 没有，但建议不超过 20 字。

### Q6: 能修改自定义选项吗？
**A**: 暂不支持直接修改。可以删除后重新添加。

---

## 🎨 视觉区分

### 选项类型一眼识别

```
┌──────────────────────────────────────┐
│ 默认选项 (灰色边框)                  │
│ [中餐]  [日料]  [西餐]              │
│
│ 自定义选项 (橙色背景)                │
│ [素食❌]  [火锅❌]  [小龙虾❌]      │
│
│ 已选中状态 (黄色背景)                │
│ [✓选中] [已选❌]                    │
└──────────────────────────────────────┘
```

---

## 🔍 故障排除

### 问题 1: 添加后看不到新选项

**解决方案**:
1. 确保输入不为空
2. 按 Enter 或点击 ✓ 确认
3. 刷新页面试试

### 问题 2: 在另一个设备看不到自定义选项

**解决方案**:
1. 检查是否登录相同账户（匿名登录）
2. 确保网络正常
3. 尝试刷新页面
4. 等待 30 秒让云端同步完成

### 问题 3: 删除按钮不显示

**解决方案**:
1. 只有自定义选项有删除按钮
2. 默认选项是受保护的，没有删除按钮
3. 确认是否是自定义选项（橙色背景）

### 问题 4: 输入框无法打开

**解决方案**:
1. 找到列表末尾的 ➕ 按钮
2. 确保字段有预设选项（无选项的字段不支持自定义）
3. 尝试刷新页面

---

## 💬 使用建议

### ✅ 好的做法

```
✓ 根据实际需要添加选项
✓ 保持选项名称简洁清晰
✓ 定期检查和清理过期选项
✓ 在多设备间同步后再删除
```

### ❌ 要避免的

```
✗ 添加重复的选项（系统会自动去重）
✗ 输入过长的选项名称
✗ 在网络不稳定时频繁操作
✗ 依赖手机缓存做唯一备份
```

---

## 🎓 高级用法

### 批量添加选项（手动）

```
虽然没有批量导入功能，但你可以快速连续添加：

[➕添加] → "素食" → Enter
[➕添加] → "火锅" → Enter  
[➕添加] → "烧烤" → Enter

都会立即保存！
```

### 跨场景共享选项

```
注意：自定义选项是按字段存储的，不同字段独立管理

例如:
- menu 字段的 "素食"
- activity 字段的 "素食"
是两个不同的选项（各自独立）

如需在多个字段使用相同名称，需分别添加。
```

---

## 📊 数据结构（仅供参考）

如果你想了解更多技术细节：

```json
{
  "customTags": {
    "menu": ["素食", "小龙虾", "精酿啤酒"],
    "activity": ["遛狗", "健身"],
    "stops": ["创意园区", "美术馆"],
    "mood": ["我的自定义情绪1"],
    ...
  }
}
```

数据存储位置：
- 本地: `localStorage` → Key: `journal-custom-tags`
- 云端: CloudBase → Collection: `user_settings` → Field: `customTags`

---

## 📞 需要帮助？

### 检查清单

遇到问题时，按顺序检查：

- [ ] 是否有网络连接？
- [ ] 是否在相同账户登录？
- [ ] 是否给了足够的同步时间？
- [ ] 浏览器是否支持 localStorage？
- [ ] 是否超过浏览器存储限额？

### 快速恢复

如果数据出现问题：
1. 清除浏览器缓存（开发者工具 → Application → Clear）
2. 重新打开应用（重新登录）
3. 云端数据会自动加载

---

## 🎉 就是这样！

现在你已经掌握了自定义选项功能的所有要点。开始使用吧！

```
朋友聚会 → 菜系/酒水 → [➕添加] → 输入你的偏好 → Enter

完成！✓
```

有任何问题，参考本文档或查看完整文档 `CUSTOM_OPTIONS_FEATURE.md`。

---

**💡 提示**: 定期检查你的自定义选项列表，删除不再需要的条目，保持列表整洁！

**⏱️ 最后更新**: 2024-01-15  
**📌 版本**: v1.0.0
