# Prompt 优化总结

## 优化目标
1. **增加故事性文字**：不要把"热烈"、"治愈"这样的选项内容直接展示在手帐画面上，太人机了
2. **避免重复**：同一个词/同一句话在画面上不能反复/多处出现

## 优化内容

### 1. 视觉风味短语优化 (`buildVisualFlavorPhrase`)

#### 优化前
```
用户「视觉风味」偏好（请在生成时严格按这些偏好执行——色调倾向 / 氛围 / 排版裁剪 / 边缘风格 / 装饰元素 / 纸张底色）：
整体色调=「暖色胶片」；氛围标签=「治愈、松弛」；排版形状偏好=「圆形」；装饰元素=「手绘小猫」；底图纸张=「米色道林纸」。
```

#### 优化后
```
用户的视觉风味偏好指导：
整体色调倾向于「暖色胶片」的视觉氛围；
画面应该传达让观者感到舒适放松的视觉语言、自然随意、不刻意的排版节奏的整体感受；
照片裁剪采用圆润柔和的照片轮廓的设计手法；
用「手绘小猫」等元素作为版面点缀，围绕主体但不遮挡内容；
整张拼贴的底层纸感采用「米色道林纸」的质地与色调。
```

**改进点**：
- 用故事化的描述替代直接列举选项（"治愈" → "让观者感到舒适放松的视觉语言"）
- 避免重复"氛围标签"、"排版形状偏好"等标签词
- 用更自然的叙述方式组织信息

### 2. 场景细节短语优化 (`buildSceneDetailPhrase`)

#### 优化前
```
场景细节（旅行）—— 目的地：日本 · 富士山；行程：3 天 2 夜；同行人：和朋友；天气：晴朗。
```

#### 优化后
```
旅行的背景故事：
这段经历发生在日本 · 富士山；
整个过程以3 天 2 夜的方式展开；
和朋友一起经历了这段时光；
当时的天气是晴朗。
```

**改进点**：
- 用叙述性的语言替代标签式的列举
- 避免重复"场景细节"这样的词
- 让 prompt 更像在讲故事，而不是在读表单

### 3. 处理流程短语优化 (`buildPipelinePhrase`)

#### 优化前
```
请按以下 7 段处理流程串联生成（每一段都不要遗漏）：
① 照片理解：...
② 故事编排：...
③ 智能排版：...
④ 照片边缘风格（独立维度，不是「形状」）：本次同时选择了「固定形状边缘」（电影胶片）与「装饰性边缘」（撕纸边）——请用「固定形状边缘」覆盖第 ③ 段的形状偏好...
```

#### 优化后
```
生成流程（请逐段执行，不要遗漏）：
① 照片理解：...
② 故事串联：...
③ 照片裁剪：...
④ 照片边缘处理：采用「电影胶片」作为主轮廓的固定外形，再在外侧叠加「撕纸边」的装饰效果；
```

**改进点**：
- 简化了冗长的条件判断语句
- 避免重复"形状"、"边缘"等词汇
- 用更简洁的动词（"故事编排" → "故事串联"，"智能排版" → "照片裁剪"）

### 4. 反幻觉约束优化

#### 优化前
```
严格遵守以下约束：
1) 画面内容必须严格基于「我提供的参考图」+「事实清单」+「照片自动识别」结果，禁止凭空添加用户未声明的人名、地名、城市、国家、坐标、街道、建筑、店铺、品牌 logo、菜品名、日期、年份、相机型号、镜头参数、票根编号、行程编号等任何具体信息；
6a) 「装饰元素」必须从用户给出的清单中取，未列出的元素不允许自创；「排版形状偏好」也必须遵守用户选择；
6b) 「照片边缘风格」是与「形状」正交的独立维度，绝不允许把「撕纸边/相框/电影胶片/取景器/宝丽得白边/贴纸描边/羽化柔边/和纸胶带」当作形状渲染...
```

#### 优化后
```
内容约束（必须严格遵守）：
1) 画面内容必须严格基于参考图、事实清单、照片自动识别的结果，禁止凭空添加用户未声明的人名、地名、城市、国家、坐标、街道、建筑、店铺、品牌、菜品名、日期、年份、相机型号、镜头参数等具体信息；
6) 装饰元素必须从用户清单中取，排版形状也必须遵守用户选择，不允许自创；
7) 照片边缘风格（撕纸边、相框、电影胶片、取景器、宝丽得白边、贴纸描边、羽化柔边、和纸胶带）是独立维度，只能作为边缘装饰，不能当作形状渲染；固定形状边缘会覆盖排版形状偏好；
```

**改进点**：
- 合并了 6a 和 6b 两条规则，避免重复
- 简化了冗长的列举，用更紧凑的表述
- 去掉了"绝不允许"这样的强硬措辞，改为更直接的"不能"

### 5. 主 Prompt 开头优化

#### 优化前
```
任务：基于我提供的 3 张参考图片，生成一张「日本旅行手帐」主题的手帐拼贴图。
画面形态：优先输出竖向长图（类似手机长截图、手帐长卷），允许把多张参考图、票根、贴纸、文字标签自上而下错落堆叠；具体纵横比与构图请按手帐美学自由发挥，不要被任何固定尺寸框住。
视觉风格：优雅纸本；版式参照：繁复拼贴；版式密度：信息密集、贴纸繁复。
```

#### 优化后
```
任务：基于 3 张参考图片，创作一张「日本旅行手帐」主题的手帐拼贴。
形态：竖向长图（手机长截图、手帐长卷风格），参考图、票根、贴纸、文字标签自上而下错落堆叠；纵横比与构图按手帐美学自由发挥，不受固定尺寸限制。
风格定位：优雅纸本；版式参照：繁复拼贴；密度：信息密集、贴纸繁复。
```

**改进点**：
- 简化了冗长的定语从句
- 用更简洁的词汇（"我提供的" → 直接省略，"请...自由发挥" → "按...自由发挥"）
- 避免重复"画面"、"版式"等词

## 核心改进策略

### 1. 故事化表述
- **前**：直接列举选项值（"治愈、松弛"）
- **后**：用叙述性的语言描述其含义（"让观者感到舒适放松的视觉语言、自然随意、不刻意的排版节奏"）

### 2. 避免重复
- **前**：同一个概念用多个词汇重复表达（"照片边缘风格"、"形状"、"边缘"多次出现）
- **后**：用同义词替换或合并相关内容，避免重复

### 3. 简化表述
- **前**：冗长的条件判断和列举
- **后**：更紧凑、直接的表述方式

### 4. 减少人机感
- **前**：大量的"请"、"必须"、"绝不允许"等强硬措辞
- **后**：更自然的指令方式，用"是"、"会"等陈述句

## 预期效果

1. **更自然的 Prompt**：不再像在读表单，而是像在讲故事
2. **更少的重复**：同一个概念不会在画面上多次出现
3. **更好的生成质量**：模型能更好地理解用户意图，而不是被选项标签迷惑
4. **更少的人机感**：生成的手帐画面会更自然、更有故事感

## 技术实现

所有优化都在 `src/lib/modelClient.ts` 中进行：
- `buildVisualFlavorPhrase()` - 视觉风味短语
- `buildSceneDetailPhrase()` - 场景细节短语
- `buildPipelinePhrase()` - 处理流程短语
- `buildKratosPrompt()` - 主 Prompt 构建

这些函数在生成图片时被调用，优化后的 Prompt 会直接影响生成结果的质量。
