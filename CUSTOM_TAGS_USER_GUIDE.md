# 自定义标签使用指南

## 什么是自定义标签？

自定义标签功能允许你在表单中添加和删除标签，以便更好地表达你的想法和感受。系统会记住你添加的所有自定义标签，即使刷新页面也不会丢失。

## 支持自定义标签的字段

目前以下字段支持自定义标签编辑：

- **情绪** - 添加你独特的情绪描述
- **氛围** - 添加你想要的氛围标签

## 如何添加新标签

### 步骤 1：打开表单
点击"开始画手帐"按钮打开补充信息表单。

### 步骤 2：找到支持自定义标签的字段
向下滚动找到"情绪"或其他支持自定义标签的字段。

### 步骤 3：点击"添加"按钮
在标签列表的最后，你会看到一个"添加"按钮。点击它。

### 步骤 4：输入新标签
一个输入框会出现。输入你想要的标签名称。

### 步骤 5：确认添加
- 按 **Enter** 键，或
- 点击输入框右侧的 **✓** 按钮

新标签会立即出现在标签列表中，并且会被自动保存。

## 如何删除标签

### 删除自定义标签
1. 找到你想删除的自定义标签
2. 点击标签右侧的 **×** 按钮
3. 标签会立即被删除

### 删除默认标签
1. 找到你想删除的默认标签
2. 如果标签右侧显示 **×** 按钮，说明可以删除
3. 如果标签右侧显示 **🔒** 图标，说明无法删除（因为至少需要保留 2 个默认标签）
4. 点击 **×** 按钮删除标签

## 如何选择标签

1. 点击任何标签即可选择它
2. 选中的标签会变成黄色高亮
3. 再次点击可以取消选择
4. 你可以同时选择多个标签

## 标签的保存和恢复

### 自动保存
- 你添加的所有自定义标签会自动保存到浏览器
- 即使关闭浏览器或刷新页面，自定义标签也会保留

### 保存到模板
- 当你保存一个模板时，该模板中的自定义标签也会被保存
- 下次应用这个模板时，自定义标签会被恢复

### 应用模板
- 当你应用一个保存的模板时，模板中的所有自定义标签会被恢复
- 这样你可以快速重用之前创建的标签组合

## 常见问题

### Q: 为什么我无法删除某个标签？
A: 系统要求每个字段至少保留 2 个默认标签。如果标签右侧显示 🔒 图标，说明删除它会导致默认标签少于 2 个，所以无法删除。

### Q: 我添加的标签会保存多久？
A: 你添加的自定义标签会一直保存在浏览器中，直到你手动删除它们或清除浏览器数据。

### Q: 我可以在不同的设备上使用我的自定义标签吗？
A: 不可以。自定义标签只保存在你当前使用的浏览器中。如果你在另一台设备上使用，需要重新添加标签。

### Q: 如果我不小心删除了一个标签怎么办？
A: 你可以重新添加它。点击"添加"按钮，输入相同的标签名称即可。

### Q: 我可以编辑已有的标签吗？
A: 目前不支持编辑标签名称。如果需要修改，可以删除旧标签，然后添加新标签。

## 最佳实践

1. **定期整理** - 定期删除不再使用的自定义标签，保持列表整洁
2. **有意义的名称** - 使用清晰、有意义的标签名称，便于日后识别
3. **保存模板** - 如果你经常使用某些标签组合，可以保存为模板以便快速应用
4. **备份重要配置** - 如果你有重要的标签组合，可以通过保存模板来备份

## 获取帮助

如果你遇到任何问题或有建议，请联系开发团队。
