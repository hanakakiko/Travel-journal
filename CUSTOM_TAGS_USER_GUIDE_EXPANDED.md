# 自定义标签功能使用指南

## 功能概述

现在，你可以为以下所有表单选项添加自定义标签：

| 选项 | 位置 | 说明 |
|------|------|------|
| **情绪** | 故事纸条 | 描述今天的心情（如：开心、平静、兴奋） |
| **氛围** | 视觉风味 | 描述整体的视觉氛围（如：治愈、松弛、复古） |
| **排版形状** | 视觉风味 | 照片的裁剪形状（如：圆形、爱心、异形） |
| **照片边缘风格** | 视觉风味 | 照片边缘的装饰风格（如：撕纸边、胶片、宝丽得） |
| **装饰元素** | 视觉风味 | 画面中的装饰元素（如：小猫、纸飞机、星星） |

## 如何添加自定义标签

### 步骤 1：打开表单
点击"开始画手帐"按钮，打开补充信息表单。

### 步骤 2：找到要添加标签的选项
向下滚动到"视觉风味"部分，找到你想添加标签的选项（如"排版形状"）。

### 步骤 3：点击"添加"按钮
在标签列表的最后，你会看到一个"添加"按钮。点击它。

### 步骤 4：输入新标签
一个输入框会出现。输入你想添加的标签名称。

### 步骤 5：确认添加
按 **Enter** 键或点击 ✓ 按钮来确认添加。

### 步骤 6：使用新标签
新标签会立即出现在列表中，你可以点击它来选择。

## 示例

### 例子 1：添加自定义排版形状
1. 打开表单，滚动到"视觉风味"部分
2. 找到"排版形状（多选 · 仅控制照片轮廓）"
3. 点击最后的"添加"按钮
4. 输入"圆角方形"
5. 按 Enter 确认
6. 现在你可以选择"圆角方形"这个新标签

### 例子 2：添加自定义装饰元素
1. 在"装饰元素（多选）"部分点击"添加"
2. 输入"樱花"
3. 按 Enter 确认
4. "樱花"会被添加到装饰元素列表中

## 如何删除自定义标签

### 步骤 1：找到要删除的标签
在标签列表中找到你想删除的自定义标签（自定义标签会有不同的样式）。

### 步骤 2：点击删除按钮
在标签上点击 **×** 按钮。

### 步骤 3：确认删除
标签会立即被删除。

## 重要提示

### ✅ 可以做的事
- ✅ 添加任意数量的自定义标签
- ✅ 删除自己添加的自定义标签
- ✅ 同时使用默认标签和自定义标签
- ✅ 自定义标签会被保存，刷新页面后仍然存在
- ✅ 保存模板时，自定义标签也会被保存

### ❌ 不能做的事
- ❌ 删除所有默认标签（至少要保留 2 个）
- ❌ 添加空标签或重复标签
- ❌ 删除默认标签（只能取消选择）

## 标签识别

### 默认标签
- 来自系统预设
- 无法删除（但可以取消选择）
- 有时会显示 🔒 图标表示受保护

### 自定义标签
- 由你添加
- 可以随时删除
- 通常有不同的视觉样式（如不同的背景色）

## 数据保存

### 自动保存
- 所有自定义标签都会自动保存到浏览器的本地存储
- 你不需要手动保存
- 关闭浏览器后，标签仍然会被保留

### 模板保存
- 当你保存一个模板时，该模板中使用的自定义标签也会被保存
- 加载模板时，自定义标签会被恢复

## 常见问题

### Q: 我添加的标签去哪了？
A: 自定义标签会自动保存到浏览器的本地存储。如果你清除了浏览器缓存，标签可能会被删除。

### Q: 我可以删除所有默认标签吗？
A: 不可以。系统要求至少保留 2 个默认标签，以确保有基本的选项可用。

### Q: 我可以编辑已添加的标签吗？
A: 目前不支持编辑。如果你想修改标签，可以删除旧标签，然后添加新标签。

### Q: 自定义标签会影响生成结果吗？
A: 是的。自定义标签会被包含在发送给 AI 的 prompt 中，可能会影响生成结果。

### Q: 我可以在不同的浏览器中使用相同的自定义标签吗？
A: 不可以。自定义标签只保存在当前浏览器的本地存储中。如果你想在其他浏览器中使用相同的标签，需要重新添加。

## 最佳实践

### 1. 使用有意义的标签名
- ✅ 好：「圆角方形」、「樱花」、「手绘风」
- ❌ 不好：「a」、「123」、「xxx」

### 2. 避免重复
- 在添加新标签前，检查是否已经存在类似的标签
- 系统会自动防止完全相同的标签重复

### 3. 定期整理
- 删除不再使用的标签，保持列表整洁
- 这样可以更快地找到你需要的标签

### 4. 与模板配合使用
- 为不同的风格创建模板，并保存相应的自定义标签
- 下次使用时，可以快速加载整个配置

## 技术细节（可选）

### 存储位置
- 自定义标签存储在浏览器的 `localStorage` 中
- 键名：`journal-custom-tags`
- 格式：JSON 对象

### 支持的字段
```json
{
  "mood": ["自定义情绪1", "自定义情绪2"],
  "vibes": ["自定义氛围1"],
  "layoutShapes": ["自定义形状1"],
  "edgeStyles": ["自定义边缘风格1"],
  "decorations": ["自定义装饰元素1"]
}
```

## 反馈和建议

如果你有任何关于自定义标签功能的建议或遇到问题，请联系开发团队。

---

**最后更新**：2024 年
**功能版本**：1.0
