# COS 上传失败重试功能 - 测试指南

## 功能测试步骤

### 场景 1：正常上传（无需重试）
1. 打开应用
2. 选择一张或多张图片上传
3. 如果 COS 上传成功，图片会正常显示，无错误提示

### 场景 2：上传失败后重试成功
1. 打开应用
2. 选择图片上传
3. 如果 COS 上传失败（例如网络问题、CORS 配置问题等）：
   - 会显示错误提示弹窗
   - 弹窗中会列出失败的图片和错误原因
   - **关键：** 弹窗底部会显示"重试上传"按钮（黄色）和"关闭"按钮
4. 点击"重试上传"按钮
5. 应用会自动重新上传失败的图片
6. 如果重试成功：
   - 弹窗关闭
   - 播放成功音效
   - 图片列表更新，显示上传成功的图片

### 场景 3：上传失败后重试仍然失败
1. 按照场景 2 的步骤 1-4 操作
2. 点击"重试上传"按钮
3. 如果重试仍然失败：
   - 显示新的错误提示弹窗
   - 列出仍然失败的图片
   - **关键：** 仍然保留"重试上传"按钮，允许用户继续重试
4. 用户可以：
   - 继续点击"重试上传"再次尝试
   - 点击"关闭"关闭弹窗，然后在"补充信息"面板手动填入图片链接

### 场景 4：部分图片失败
1. 选择多张图片上传
2. 如果其中部分图片上传失败：
   - 成功的图片会正常显示
   - 失败的图片会在错误提示中列出
   - 点击"重试上传"只会重新上传失败的图片，不会重复上传成功的图片

## 预期行为

### 错误提示弹窗的按钮布局
```
┌─────────────────────────────────────┐
│ ⚠️  [关闭按钮]                        │
├─────────────────────────────────────┤
│ 3 张图片上传失败，无法被 AI 使用：    │
│                                     │
│ 1. photo1.jpg — COS 上传失败 HTTP 403 │
│ 2. photo2.jpg — COS 上传被浏览器拦截  │
│ 3. photo3.jpg — COS 上传 60 秒内未完成 │
│                                     │
│ 解决方案：                           │
│ 1. 在「补充信息」面板手动填入...      │
│ 2. 删除这些图片后重新上传             │
│ 3. 继续生成但结果可能不会用到...      │
├─────────────────────────────────────┤
│ [重试上传] [关闭]                    │
└─────────────────────────────────────┘
```

### 按钮样式
- **重试上传**：黄色背景（`var(--accent-main)`），黑色文字，左侧
- **关闭**：黑色背景，白色文字，右侧
- 悬停时：按钮向上移动 2px，阴影增加
- 点击时：按钮回到原位置，阴影减少

## 调试技巧

### 模拟上传失败
如果想测试重试功能但 COS 上传正常，可以：

1. **修改 COS 配置**：在 `.env` 中设置错误的 COS 地址
2. **禁用网络**：使用浏览器开发者工具的网络限流功能
3. **检查浏览器控制台**：查看 `[COS]` 前缀的日志信息

### 查看日志
在浏览器控制台中，会看到类似的日志：
```
[COS] PUT → https://journal-photos-1302323802.cos.ap-shanghai.myqcloud.com/journal/20260604/xxx.jpg
[COS] × upload failed photo.jpg Error: COS 上传失败 HTTP 403
```

## 相关配置

### COS 配置
- 基础 URL：`VITE_COS_PUT_BASE`（默认：`https://journal-photos-1302323802.cos.ap-shanghai.myqcloud.com`）
- 路径前缀：`VITE_COS_PATH_PREFIX`（默认：`journal/`）
- 超时时间：`DEFAULT_TIMEOUT_MS`（默认：60000ms）

### 开发环境
- Dev 代理：`/cos-upload`（自动启用，无需 CORS 配置）
- 生产环境：需要在 COS 控制台配置 CORS 规则

## 常见问题

### Q: 重试时会重新压缩图片吗？
A: 是的。重试时会重新调用 `processImageFile()`，包括重新压缩图片。这确保了最新的图片数据被上传。

### Q: 重试时会保留原来的 EXIF 信息吗？
A: 是的。EXIF 信息是从本地 blob 中提取的，重试时会重新解析。

### Q: 如果用户在重试过程中关闭浏览器会怎样？
A: 重试过程中的图片数据会丢失。用户需要重新上传。

### Q: 重试按钮什么时候会显示？
A: 只有当 `failedPhotosForRetry` 数组不为空时，重试按钮才会显示。这确保了按钮只在有失败图片时出现。

## 性能考虑

- 重试时会重新处理图片（包括压缩），可能需要几秒钟
- 大量图片重试时，建议分批处理
- 网络状态不稳定时，可能需要多次重试

## 相关代码位置

- 错误提示组件：`src/lib/ErrorAlert.tsx`
- 重试逻辑：`src/App.tsx` 中的 `retryFailedPhotos()` 函数
- 样式定义：`src/styles.css` 中的 `.error-alert-retry` 类
- COS 上传：`src/lib/cosUploader.ts`
- 图片处理：`src/lib/imageTools.ts`
