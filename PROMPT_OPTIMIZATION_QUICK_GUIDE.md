# Prompt 优化快速指南

## 🎯 优化目标
1. **增加故事性** - 不要直接展示"热烈"、"治愈"这样的选项标签
2. **避免重复** - 同一个词/句子不能在画面上多处出现

## 📝 核心改进

### 问题 1：选项标签直接展示（太人机）

**优化前**
```
氛围标签=「治愈、松弛」
排版形状偏好=「圆形」
装饰元素=「手绘小猫」
```

**优化后**
```
画面应该传达让观者感到舒适放松的视觉语言、自然随意、不刻意的排版节奏的整体感受
照片裁剪采用圆润柔和的照片轮廓的设计手法
用「手绘小猫」等元素作为版面点缀，围绕主体但不遮挡内容
```

**改进策略**：
- 用 `vibeDescriptions` 映射表将选项转化为描述性语言
- 用 `shapeDescriptions` 映射表将形状转化为设计语言
- 避免直接列举选项值

---

### 问题 2：重复词汇（同一个词多次出现）

**优化前**
```
场景细节（旅行）—— 目的地：日本；行程：3天；同行人：朋友；天气：晴朗。
```

**优化后**
```
旅行的背景故事：
这段经历发生在日本；
整个过程以3天的方式展开；
和朋友一起经历了这段时光；
当时的天气是晴朗。
```

**改进策略**：
- 根据字段类型用不同的叙述方式
- 避免重复"场景细节"这样的标签词
- 用自然的叙述句式替代标签式列举

---

### 问题 3：处理流程中的冗长条件判断

**优化前**
```
④ 照片边缘风格（独立维度，不是「形状」）：本次同时选择了「固定形状边缘」（电影胶片）与「装饰性边缘」（撕纸边）——请用「固定形状边缘」覆盖第 ③ 段的形状偏好按其固有外形...
```

**优化后**
```
④ 照片边缘处理：采用「电影胶片」作为主轮廓的固定外形，再在外侧叠加「撕纸边」的装饰效果；
```

**改进策略**：
- 简化冗长的条件判断语句
- 用更简洁的动词（"处理"替代"风格"）
- 避免重复"形状"、"边缘"等词汇

---

## 🔧 技术实现

### 1. 视觉风味映射表
```typescript
const vibeDescriptions: Record<string, string> = {
  "治愈": "让观者感到舒适放松的视觉语言",
  "松弛": "自然随意、不刻意的排版节奏",
  "热烈": "饱满充沛的色彩与情绪表达",
  // ...
};
```

### 2. 形状映射表
```typescript
const shapeDescriptions: Record<string, string> = {
  "圆形": "圆润柔和的照片轮廓",
  "爱心": "充满温情的心形裁剪",
  // ...
};
```

### 3. 场景细节叙述
```typescript
if (label === "目的地") {
  narratives.push(`这段经历发生在${value}`);
} else if (label === "天气") {
  narratives.push(`当时的天气是${value}`);
}
```

---

## 📊 优化效果对比

| 维度 | 优化前 | 优化后 |
|------|------|------|
| **人机感** | 高（直接列举选项） | 低（故事化表述） |
| **重复度** | 高（同词多次出现） | 低（用同义词替换） |
| **可读性** | 中（标签式） | 高（叙述式） |
| **生成质量** | 中（模型易被选项迷惑） | 高（模型理解意图更清晰） |

---

## 🎨 生成结果预期

### 优化前的问题
- 手帐画面上直接出现"治愈"、"热烈"这样的选项标签
- 同一个词（如"圆形"）在多个地方重复出现
- 整体感觉像是在读表单，而不是在看故事

### 优化后的改进
- 手帐画面更自然，选项标签被转化为视觉表现
- 避免了词汇重复，画面更清爽
- 整体感觉像是在讲故事，而不是在读表单

---

## 📂 修改文件

所有优化都在 `src/lib/modelClient.ts` 中：

| 函数 | 改进内容 |
|------|--------|
| `buildVisualFlavorPhrase()` | 用映射表将选项转化为描述性语言 |
| `buildSceneDetailPhrase()` | 用叙述式语言替代标签式列举 |
| `buildPipelinePhrase()` | 简化冗长的条件判断，避免重复 |
| `buildKratosPrompt()` | 简化主 prompt 开头，避免重复 |

---

## 🚀 使用建议

1. **测试生成效果**：用相同的输入参数生成图片，对比优化前后的结果
2. **收集反馈**：观察生成的手帐画面是否更自然、更有故事感
3. **迭代优化**：根据实际生成效果，继续调整映射表中的描述文案

---

## 📌 关键要点

✅ **做了什么**
- 将选项标签转化为描述性语言
- 用叙述式语言替代标签式列举
- 简化冗长的条件判断
- 避免词汇重复

❌ **没有改变**
- 核心逻辑和功能
- 约束条件和规则
- 生成流程和步骤

💡 **核心思想**
> 让 prompt 更像在讲故事，而不是在读表单；让生成的手帐更自然，而不是太人机。
