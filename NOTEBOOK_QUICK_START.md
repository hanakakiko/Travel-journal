# 手帐展示架 - 快速入门指南

## 📚 什么是手帐展示架？

手帐展示架是一个功能完整的手帐收藏系统，让用户能够：
- 创建多个主题的"手帐本"（如"2024年秋季"、"日本之旅"等）
- 在每个手帐本中收集生成的手帐页面
- 随时查看、整理、排序和删除页面
- 生成完成后快速保存到指定的手帐本

## 🎯 核心功能

### 1️⃣ 创建手帐本
```
位置: 顶部菜单 → "我的手帐本" → "新建手帐本"
步骤:
  1. 输入手帐本名称（如："2024年冬日回忆"）
  2. 上传一张封面图作为手帐本的代表
  3. 点击"创建"完成
```

### 2️⃣ 查看手帐本
```
位置: 手帐展示架页面
特点:
  - 所有手帐本以卡片形式展示
  - 显示封面图和页面数量
  - 点击卡片打开手帐本详情
```

### 3️⃣ 管理手帐页面
```
位置: 打开手帐本后
可操作:
  ✓ 上传新页面      - 点击 "上传新页面" 按钮选择图片
  ✓ 查看页面        - 点击页面图片全屏查看
  ✓ 删除页面        - 悬停页面，点击删除按钮
  ✓ 调整页面顺序    - 拖动页面到新位置
```

### 4️⃣ 生成后快速保存
```
位置: 手帐生成完成后
步骤:
  1. 在生成结果上点击 "保存到手帐本" 按钮
  2. 选择要保存到的手帐本
  3. 点击 "保存" → 自动添加到手帐本
```

## 🗂️ 数据存储

所有数据存储在 **腾讯云 CloudBase** 中，与用户账户绑定：

| 数据类型 | 存储位置 | 特点 |
|---------|--------|------|
| 手帐本信息 | CloudBase 数据库 | 包含名称、封面图、创建时间 |
| 手帐页面 | CloudBase 数据库 | 包含图片 URL、标题、排序 |
| 图片文件 | 暂存为 DataURL | ⚠️ 刷新后丢失，需实现 COS 上传 |

## 💡 使用示例

### 场景 1：整理旅游回忆
```
1. 创建手帐本 "日本之旅 2024"
2. 上传旅游照片作为封面
3. 生成 5 张手帐页面，分别保存到这个手帐本
4. 在手帐本中查看所有页面，调整显示顺序
5. 下次打开时，所有回忆都整齐地保存在这里
```

### 场景 2：创建月度手帐
```
1. 按月份创建手帐本
   - "2024年10月"
   - "2024年11月"
   - "2024年12月"
2. 每月生成手帐时，选择对应的月份本
3. 年底回顾时，快速浏览整年的手帐
```

## ⚙️ 技术细节

### 文件上传限制
- **大小**: 最大 10MB
- **格式**: 支持所有常见图片格式 (JPEG, PNG, WebP, GIF 等)
- **上传方式**: 暂存为 DataURL (刷新后丢失)

### 性能特点
- 页面数量少时响应快速
- 支持拖放排序
- 实时数据同步（通过 CloudBase）

### 浏览器兼容性
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

## 🔧 常见操作

### 如何重命名手帐本？
**当前版本不支持，可在 `NotebookDetailModal` 中添加编辑功能**

### 如何恢复删除的页面？
**当前版本不支持撤销，删除前需确认**

### 如何导出手帐本？
**当前版本不支持，可在后续版本中实现 PDF 导出**

### 如何分享手帐本？
**当前版本为个人私有，可在后续版本中实现分享功能**

## 📋 检查清单：使用前必读

- [ ] 已登录账户
- [ ] CloudBase 已初始化（自动完成）
- [ ] 网络连接正常
- [ ] 浏览器最新版本
- [ ] 图片文件不超过 10MB

## 🚀 开始使用

1. **打开应用**
   - 在顶部菜单找到 "我的手帐本"

2. **第一次使用**
   - 点击 "新建手帐本"
   - 选择一张喜欢的照片作为封面
   - 输入有意义的名称

3. **上传第一个页面**
   - 点击手帐本卡片打开
   - 点击 "上传新页面"
   - 选择一张生成的手帐图片

4. **整理页面**
   - 拖动页面调整顺序
   - 删除不满意的页面
   - 继续添加新页面

## ⚠️ 重要注意事项

### 数据持久化 ⚠️
- 生成的图片 URL 当前存储为 DataURL
- **页面刷新后 DataURL 会丢失**
- 需要实现真实的 COS 存储以持久化
- 详见 `NOTEBOOK_SHELF_FEATURE.md` 中的后续开发任务

### 隐私和安全 🔒
- 每个用户只能看到自己的手帐本
- 删除操作需要确认，无法恢复
- 所有数据存储在腾讯云服务器

## 📞 故障排除

### 问题：点击"我的手帐本"没有反应
**解决:**
- 检查网络连接
- 刷新页面重试
- 查看浏览器控制台是否有错误（F12 → Console）

### 问题：上传页面后看不到图片
**解决:**
- 确保网络连接正常
- 刷新页面查看
- 确认图片文件完整（不超过 10MB）

### 问题：拖放页面无法保存
**解决:**
- 检查网络连接
- 等待几秒让操作完成
- 刷新页面验证排序是否已保存

### 问题：删除手帐本后想恢复
**解决:**
- ❌ 无法恢复，已删除的数据会永久丢失
- 建议定期备份重要的手帐本

## 🎓 学习资源

- **如何更好地使用手帐**：见 `NOTEBOOK_SHELF_FEATURE.md`
- **技术实现细节**：见 `src/lib/notebookManager.ts`
- **组件源码**：见 `src/components/Notebook*.tsx`

## 🎉 开心使用！

现在，去创建你的第一本手帐本，记录生活中的美好时刻吧！✨

---

如有问题或建议，欢迎提交 Issue 或 PR！
