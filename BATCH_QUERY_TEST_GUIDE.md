# 分批查询优化 - 测试指南

## 快速测试步骤

### 1. 打开浏览器开发者工具

在应用运行时，打开浏览器的开发者工具（F12 或 Cmd+Option+I），切换到 **Console** 标签页。

### 2. 进入"我的手帐本"页面

1. 点击应用中的"我的手帐本"菜单
2. 确保已经创建了至少一个手帐本

### 3. 点击某个手帐本进入详情页面

点击任何一个手帐本卡片，打开手帐本详情弹窗。

### 4. 观察控制台日志

在浏览器控制台中，你应该看到类似以下的日志输出：

```
[getPagesByNotebook] 手帐本 notebook_xxx 共有 13 个页面，将分 3 批查询
[getPagesByNotebook] 查询第 1/3 批（跳过 0 条，查询 5 条）
[getPagesByNotebook] 第 1/3 批获取了 5 个页面
[getPagesByNotebook] 查询第 2/3 批（跳过 5 条，查询 5 条）
[getPagesByNotebook] 第 2/3 批获取了 5 个页面
[getPagesByNotebook] 查询第 3/3 批（跳过 10 条，查询 5 条）
[getPagesByNotebook] 第 3/3 批获取了 3 个页面
```

## 验证清单

### ✅ 基本功能验证

- [ ] 手帐本详情页面能够正常加载
- [ ] 所有页面都能正确显示（没有遗漏或重复）
- [ ] 页面按正确的顺序排列（按 order 字段升序）
- [ ] 页面总数与手帐本信息中的 pageCount 一致

### ✅ 分批查询验证

- [ ] 控制台输出了分批查询的日志
- [ ] 日志中显示的总页数正确
- [ ] 日志中显示的批数正确（⌈总页数 / 5⌉）
- [ ] 每批查询的页数不超过 5 个

### ✅ 错误处理验证

- [ ] 如果网络中断，应该看到错误提示
- [ ] 错误提示应该清晰说明问题所在
- [ ] 应用不会因为查询失败而崩溃

### ✅ 性能验证

- [ ] 页面加载时间在可接受范围内（通常 1-3 秒）
- [ ] 没有明显的卡顿或冻结
- [ ] 内存占用在正常范围内

## 不同场景的测试

### 场景 1：小数据量（1-5 个页面）

**预期行为：**
- 只需 1 批查询
- 日志显示 `将分 1 批查询`
- 页面立即加载完成

**测试步骤：**
1. 创建一个新手帐本
2. 添加 3 个页面
3. 进入详情页面
4. 观察日志和页面加载

### 场景 2：中等数据量（6-15 个页面）

**预期行为：**
- 需要 2-3 批查询
- 日志显示 `将分 2 批查询` 或 `将分 3 批查询`
- 页面在 1-2 秒内加载完成

**测试步骤：**
1. 创建一个新手帐本
2. 添加 13 个页面（这是原始问题的场景）
3. 进入详情页面
4. 观察日志和页面加载
5. 验证所有 13 个页面都正确显示

### 场景 3：大数据量（20+ 个页面）

**预期行为：**
- 需要 4+ 批查询
- 日志显示 `将分 4 批查询` 或更多
- 页面在 2-3 秒内加载完成

**测试步骤：**
1. 创建一个新手帐本
2. 添加 25 个页面
3. 进入详情页面
4. 观察日志和页面加载
5. 验证所有 25 个页面都正确显示

### 场景 4：边界情况

**测试 4.1：恰好是 BATCH_SIZE 的倍数**
- 添加 5 个页面（恰好 1 批）
- 添加 10 个页面（恰好 2 批）
- 添加 15 个页面（恰好 3 批）

**测试 4.2：不是 BATCH_SIZE 的倍数**
- 添加 7 个页面（1 批 + 部分）
- 添加 12 个页面（2 批 + 部分）
- 添加 23 个页面（4 批 + 部分）

## 常见问题排查

### Q1：为什么看不到日志？

**A：** 检查以下几点：
1. 确保浏览器控制台已打开（F12）
2. 确保控制台过滤器没有隐藏 `[getPagesByNotebook]` 日志
3. 确保应用已经重新加载（Ctrl+R 或 Cmd+R）
4. 检查是否有其他错误导致函数没有执行

### Q2：为什么页面加载很慢？

**A：** 可能的原因：
1. 网络连接较慢 - 分批查询会增加网络往返次数
2. 数据库响应慢 - 检查 CloudBase 的性能
3. 浏览器性能问题 - 尝试关闭其他标签页

**解决方案：**
- 可以调整 `BATCH_SIZE` 参数（在 `notebookManager.ts` 中）
- 增大 `BATCH_SIZE` 可以减少查询次数，但可能增加单次查询的返回体大小
- 减小 `BATCH_SIZE` 可以减小单次返回体，但会增加查询次数

### Q3：为什么还是出现数据库错误？

**A：** 如果仍然看到以下错误：
```
"Sort operation used more than the maximum 33554432 bytes of RAM"
"查询结果超过单次回包大小限制"
```

可能的原因：
1. 页面数据太大（如包含大量文本或高分辨率图片 URL）
2. `BATCH_SIZE` 设置过大
3. CloudBase 数据库有其他限制

**解决方案：**
- 减小 `BATCH_SIZE`（如从 5 改为 3）
- 检查页面数据是否包含不必要的大字段
- 联系 CloudBase 技术支持

### Q4：页面顺序不对怎么办？

**A：** 如果页面顺序不正确：
1. 检查日志中是否有排序相关的错误
2. 尝试刷新页面（F5）
3. 检查数据库中的 `order` 字段是否正确
4. 如果问题持续，可能需要重新排序所有页面

## 性能基准

### 预期的加载时间

| 页面数量 | 批数 | 预期加载时间 |
|---------|------|-----------|
| 1-5     | 1    | < 500ms   |
| 6-10    | 2    | 500-1000ms |
| 11-15   | 3    | 1000-1500ms |
| 16-20   | 4    | 1500-2000ms |
| 21-25   | 5    | 2000-2500ms |

**注意：** 实际加载时间取决于网络速度和 CloudBase 的响应时间。

## 日志分析

### 正常的日志输出

```
[getPagesByNotebook] 手帐本 notebook_user_1234567890 共有 13 个页面，将分 3 批查询
[getPagesByNotebook] 查询第 1/3 批（跳过 0 条，查询 5 条）
[getPagesByNotebook] 第 1/3 批获取了 5 个页面
[getPagesByNotebook] 查询第 2/3 批（跳过 5 条，查询 5 条）
[getPagesByNotebook] 第 2/3 批获取了 5 个页面
[getPagesByNotebook] 查询第 3/3 批（跳过 10 条，查询 5 条）
[getPagesByNotebook] 第 3/3 批获取了 3 个页面
```

### 回退到不带 orderBy 的日志

```
[getPagesByNotebook] 第 1/3 批 orderBy 查询失败，尝试不带 orderBy: Error: ...
[getPagesByNotebook] 第 1/3 批获取了 5 个页面
```

### 回退到全量查询的日志

```
[getPagesByNotebook] 第 1/3 批 skip 查询也失败，尝试全量查询后客户端分页: Error: ...
[getPagesByNotebook] 使用全量查询方案，返回 13 个页面
```

## 提交反馈

如果在测试过程中发现任何问题，请记录以下信息：

1. **问题描述**：具体发生了什么
2. **重现步骤**：如何重现这个问题
3. **浏览器控制台日志**：完整的日志输出
4. **页面数量**：手帐本中有多少个页面
5. **网络状态**：网络连接是否正常
6. **浏览器版本**：使用的浏览器和版本号

## 相关文件

- [`src/lib/notebookManager.ts`](src/lib/notebookManager.ts) - 核心实现
- [`BATCH_QUERY_OPTIMIZATION.md`](BATCH_QUERY_OPTIMIZATION.md) - 详细的优化说明
