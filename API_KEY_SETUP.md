# 自定义 API Key 配置指南

## 概述

现在你可以使用自己的 API Key 来调用图生图模型，这样就不会消耗我的额度。应用支持两种模型：

- **GPT-2 (Kratos)** - 小红书内部 Kratos 平台的模型
- **FLUX.2 [pro]** - Black Forest Labs 的 FLUX.2 Pro 模型

## 如何配置

### 1. 打开 API 配置面板

在应用的左上角，你会看到一个设置按钮（⚙️ 图标）。点击它打开 API 配置面板。

### 2. 选择模型

在配置面板中，选择你想使用的模型：
- **GPT-2 (Kratos)** - 需要小红书 Kratos 平台的 API Token
- **FLUX.2 [pro]** - 需要 Replicate 平台的 API Token

### 3. 输入 API Key

根据你选择的模型，输入对应的 API Key：

#### 对于 FLUX.2 [pro]：
1. 访问 [Replicate](https://replicate.com) 官网
2. 注册或登录账户
3. 在 [API Token 页面](https://replicate.com/account/api-tokens) 获取你的 API Token
4. 将 Token 复制到配置面板的 "API Key" 字段

#### 对于 GPT-2 (Kratos)：
1. 获取你的 Kratos API Token（通常由小红书内部提供）
2. 将 Token 复制到配置面板的 "API Key" 字段

### 4. 自定义端点（可选）

如果你有自己的模型服务或代理，可以在 "自定义端点 URL" 字段中输入自定义的 API 端点。

- 对于 FLUX.2：通常是 `https://api.replicate.com/v1/predictions`
- 对于 GPT-2：通常是你的 Kratos 服务地址

如果留空，应用会使用默认的端点。

### 5. 保存配置

点击 "保存配置" 按钮。你的 API Key 会被保存到浏览器的本地存储中。

## 安全性说明

⚠️ **重要提示：**

- API Key 被保存在你的浏览器本地存储中，不会被发送到任何服务器
- 请不要在公共电脑上保存你的 API Key
- 如果你想清除已保存的配置，点击 "清除配置" 按钮
- 定期检查你的 API Key 使用情况，防止被滥用

## 模型选择

### GPT-2 (Kratos)
- **优点**：速度快，适合快速迭代
- **缺点**：只支持 1 张参考图
- **适用场景**：快速生成、实时预览

### FLUX.2 [pro]
- **优点**：支持最多 8 张参考图，质量最高
- **缺点**：生成速度较慢（约 30 秒）
- **适用场景**：高质量输出、多张参考图融合

## 常见问题

### Q: 我的 API Key 会被保存在哪里？
A: API Key 被保存在你的浏览器本地存储中（localStorage），不会被发送到任何远程服务器。

### Q: 如何更换 API Key？
A: 打开 API 配置面板，修改 API Key 字段，然后点击 "保存配置"。

### Q: 如何清除已保存的 API Key？
A: 打开 API 配置面板，点击 "清除配置" 按钮。

### Q: 如果我没有配置 API Key 会怎样？
A: 应用会尝试使用环境变量中的 API Key（如果有的话）。如果都没有，生成时会报错。

### Q: 我可以同时配置两个模型的 API Key 吗？
A: 不可以。配置面板一次只能配置一个模型的 API Key。如果你想切换模型，需要重新配置。

### Q: 自定义端点 URL 有什么用？
A: 如果你有自己的模型服务或代理，可以使用自定义端点来调用你自己的服务，而不是官方的 API 端点。

## 故障排除

### 生成失败，提示 "API Token 未配置"
- 检查你是否在 API 配置面板中输入了 API Key
- 确保 API Key 不为空
- 尝试清除配置后重新输入

### 生成失败，提示 "HTTP 401" 或 "Unauthorized"
- 你的 API Key 可能已过期或无效
- 检查 API Key 是否正确复制
- 在 Replicate 或 Kratos 平台上验证你的 API Key 是否仍然有效

### 生成失败，提示 "网络异常"
- 检查你的网络连接
- 如果使用了自定义端点，确保端点 URL 正确且可访问
- 尝试刷新页面后重新生成

## 更多帮助

如果你遇到问题，请：
1. 检查浏览器控制台（F12）查看详细的错误信息
2. 确保你的 API Key 有效且有足够的额度
3. 尝试使用不同的模型
4. 清除浏览器缓存后重试
