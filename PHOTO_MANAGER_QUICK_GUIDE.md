# 增删画面功能 - 快速指南

## 功能说明

### 按钮变化
- **第一次使用**：显示 "选择照片" 按钮 → 直接上传图片
- **上传图片后**：显示 "增删画面" 按钮 → 打开图片管理面板

## 图片管理面板功能

### 1. 添加新图片
- 点击 "增删画面" 按钮打开面板
- 在 "添加新图片" 区域上传新图片
- 支持拖拽上传或点击选择文件
- 支持多张图片同时上传

### 2. 删除已上传的图片
- 在 "已上传的画面" 列表中找到要删除的图片
- 将鼠标悬停在图片上，会显示 "删除" 按钮
- 点击 "删除" 按钮即可删除该图片
- 删除后会自动更新图片列表

### 3. 图片状态指示
每张图片下方显示：
- ✓ 已上传到云端 - 绿色，表示可以被 AI 使用
- ⚠ 云端上传失败 - 橙色，表示需要手动填写远程链接

## 工作流程

```
1. 初始状态
   ↓
2. 点击 "选择照片" 上传图片
   ↓
3. 图片上传完成，按钮变为 "增删画面"
   ↓
4. 点击 "增删画面" 打开管理面板
   ↓
5. 在面板中可以：
   - 添加更多图片
   - 删除不需要的图片
   - 查看每张图片的上传状态
   ↓
6. 关闭面板继续其他操作
```

## 常见操作

### 添加更多图片
1. 点击 "增删画面" 按钮
2. 在 "添加新图片" 区域选择或拖拽图片
3. 等待上传完成
4. 新图片会自动添加到列表中

### 删除错误上传的图片
1. 点击 "增删画面" 按钮
2. 找到要删除的图片
3. 将鼠标悬停在图片上
4. 点击出现的 "删除" 按钮
5. 图片立即被删除

### 替换图片
1. 删除旧图片（见上）
2. 添加新图片（见上）

## 提示

- 删除图片后无法撤销，请谨慎操作
- 删除图片会同时清除该图片的所有识别标签
- 云端上传失败的图片可以在 "补充信息" 面板中手动填写链接
- 图片列表会实时更新，无需刷新页面

## 响应式设计

- **桌面端**：网格布局，每行显示多个图片
- **平板端**：自适应列数
- **手机端**：单列或双列布局，便于操作

## 技术细节

### 删除图片时会清理的数据
1. 从图片列表中移除
2. 从远程链接列表中移除
3. 清除 VLM 识别的标签
4. 更新图片计数

### 数据一致性
- 所有相关数据会同时更新
- 不会产生孤立数据
- 删除操作是原子性的

## 故障排除

### 图片上传失败
- 检查网络连接
- 确认图片格式正确（JPG、PNG 等）
- 查看错误提示信息
- 可在 "补充信息" 面板手动填写链接

### 删除按钮不显示
- 确保鼠标悬停在图片上
- 检查浏览器是否支持 CSS hover 效果
- 尝试刷新页面

### 图片列表不更新
- 尝试关闭并重新打开面板
- 刷新页面
- 检查浏览器控制台是否有错误信息
