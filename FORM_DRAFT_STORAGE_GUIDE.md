# 表单草稿自动保存功能使用指南

## 功能概述

现在当你在填写表单时不小心切去其他页面或关闭了表单，已填写的内容会自动保存到浏览器的本地缓存中。下次打开应用时，这些内容会自动恢复，你可以继续上次的填写。

## 功能特点

### 1. **自动保存**
   - 表单内容每次变化时自动保存到浏览器本地存储
   - 包括：
     - 上传的照片列表
     - 表单填写的所有内容（场景、心情、风格等）
     - 选择的模板和样式
     - 远程图片链接

### 2. **自动恢复**
   - 打开应用时会自动检测是否有草稿
   - 如果有草稿，会自动恢复上次未完成的表单内容
   - 顶部会显示绿色提示：`✓ 已恢复上次未完成的表单内容（X 张照片）`

### 3. **草稿清理**
   - 点击提示条右上角的 `✕` 可以关闭提示（但不清除数据）
   - 点击上传区域的"清除全部"按钮可以完全清除所有表单数据
   - 删除照片时会自动清除已生成的草稿

## 工作原理

### 数据存储位置

所有草稿数据存储在浏览器的 `localStorage` 中：

| 存储键 | 存储内容 | 说明 |
|--------|--------|------|
| `form_draft_answers` | 用户回答数据 | 包含所有表单选项和填写内容 |
| `form_draft_photos` | 照片列表 | 用户上传的所有照片数据 |
| `form_draft_styleId` | 样式 ID | 选择的表单样式 |
| `form_draft_templateId` | 模板 ID | 选择的表单模板 |
| `form_draft_timestamp` | 保存时间戳 | 最后一次保存的时间 |

### 保存时机

- **自动保存**：当 `answers`、`photos`、`styleId`、`templateId` 任何一项发生变化时
- **避免保存**：生成图片或处理文件时不会保存（防止保存不完整数据）
- **清除数据**：
  - 用户点击"清除全部"按钮时
  - 生成完成后（生成成功的结果中）

### 恢复流程

1. 用户首次打开或重新打开应用
2. 应用检查 localStorage 中是否有有效的草稿数据
3. 如果存在草稿：
   - 自动加载照片和表单内容
   - 显示绿色恢复提示
   - 用户可继续编辑
4. 如果不存在草稿：
   - 显示默认的空表单

## 使用场景

### 场景 1：意外切走其他页面
```
用户正在填写表单 → 不小心点到其他应用 → 
关闭浏览器或页面 → 重新打开 → 
内容自动恢复，继续填写
```

### 场景 2：调整表单内容
```
用户上传了照片并填写了内容 → 想重新调整某些选项 → 
即使关闭页面也不会丢失 → 下次打开继续编辑
```

### 场景 3：清除旧数据
```
用户想开始新的表单 → 点击上传区的"清除全部"按钮 → 
确认后所有数据被清空 → 可以重新开始
```

## 注意事项

### ✓ 会自动保存的数据
- 照片和图片 URL
- 所有表单选项和填写的文本
- 选择的风格和模板

### ✗ 不会自动保存的数据
- 已生成的日志/图片（完成后需另外导出）
- 生成过程中的临时数据

### 浏览器兼容性
- 支持所有现代浏览器（Chrome、Firefox、Safari、Edge 等）
- 基于 `localStorage` API，容量通常为 5-10MB
- 私密/无痕模式下可能不可用

### 跨设备同步
- 数据仅保存在当前浏览器中
- 不同设备、不同浏览器的数据互不影响
- 如需跨设备使用，可考虑保存为模板

## 相关 API

如果你需要在其他地方使用草稿功能，可以使用以下 API：

```typescript
import { 
  saveFormDraft,      // 手动保存草稿
  loadFormDraft,      // 加载草稿
  clearFormDraft,     // 清除草稿
  hasFormDraft,       // 检查是否有草稿
  getFormDraftTimestamp // 获取最后保存时间
} from "./lib/formDraftStorage";

// 例子：检查是否有草稿
if (hasFormDraft()) {
  const draft = loadFormDraft();
  console.log('恢复的照片数量:', draft.photos.length);
}

// 例子：手动保存
saveFormDraft(answers, photos, styleId, templateId);

// 例子：清除所有草稿
clearFormDraft();
```

## 常见问题

### Q: 为什么看不到我之前的数据？
A: 可能的原因：
1. 浏览器的 localStorage 被清空了
2. 使用了无痕/隐私模式（数据不会保存）
3. 更换了浏览器或设备
4. 清除了浏览器缓存

### Q: 数据会一直保存吗？
A: 除非你：
1. 清除浏览器缓存/localStorage
2. 卸载浏览器
3. 手动点击"清除全部"按钮
4. 浏览器空间不足被自动清空

### Q: 如何在不同设备间同步？
A: 目前方案：
- 将表单保存为**模板**（点击"保存风格"）
- 在其他设备导入模板
- 或手动截图记录内容

### Q: localStorage 满了怎么办？
A: 应用会捕获错误并静默处理，但建议：
1. 清除浏览器缓存
2. 删除不需要的本地数据
3. 及时导出完成的内容

## 故障排除

### 草稿无法恢复
1. 检查浏览器是否允许 localStorage
2. 尝试在控制台查看 `localStorage.getItem('form_draft_answers')`
3. 清除缓存后重试

### 数据异常或损坏
1. 打开浏览器开发者工具 (F12)
2. 进入 Application/Storage > Local Storage
3. 找到该应用的 localStorage
4. 手动删除以 `form_draft_` 开头的键
5. 刷新页面重新开始

## 相关文件

- **实现文件**：`src/lib/formDraftStorage.ts`
- **集成位置**：`src/App.tsx`
- **导出工具**：`src/lib/modelClient.ts`（日志导出）

## 更新日志

### v1.0 (2024-06-06)
- ✨ 实现自动保存功能
- ✨ 实现自动恢复功能
- ✨ 添加草稿恢复提示
- ✨ 添加清除全部数据按钮
- ✨ 防止生成过程中保存不完整数据
