# 倾诉记录 Prompt 集成更新（v2）

## 需求变更

根据新的需求，倾诉记录在 prompt 中的集成方式进行了调整：

### 原需求
- 倾诉记录仅作为情绪和风格指导
- 不影响画面中文字的生成规则

### 新需求（v2）
倾诉记录支持**两个独立的勾选项**，可以组合使用：

#### 1. **勾选"融入画面生成"** ✅ （默认勾选）
- **用途**：作为风格参考和关键词提取
- **文字限制**：**去掉画面中文字只能取自用户选项的限制**
- **效果**：AI 可以根据倾诉记录的关键词和情绪自由创作文案
- **Prompt 说明**：
  ```
  用户的倾诉记录（作为情绪和风格指导，可自由提取关键词用于文案）：
  「[用户输入的倾诉内容]」。
  请根据这段话的情绪、关键词和氛围来调整画面的整体风格和文案内容，
  但不要直接引用或写出这段话的原文。
  ```

#### 2. **勾选"放在画面中"** ✅ （默认不勾选）
- **用途**：作为实际内容，必须显示在手帐画面中
- **文字限制**：保持原有限制（只能取自用户选项）
- **效果**：倾诉记录被添加到"事实清单"中，并强制要求 AI 在画面中呈现
- **Prompt 说明**：
  ```
  信息基础（仅能使用以下内容，其他视为未知）：
  主题场景：...
  叙述者口吻：...
  情绪关键词：...
  用户的倾诉记录：[用户输入的倾诉内容]
  
  【重要】用户的倾诉记录必须在画面中呈现：「[用户输入的倾诉内容]」。
  这段话是画面的核心内容，请将其以合适的形式（边注、贴纸、标签、手写感文字等）
  融入手帐拼贴中，确保观者能清晰看到这段倾诉。
  ```

#### 3. **都不勾选** ❌
- **用途**：倾诉记录不影响 prompt
- **效果**：倾诉记录仅作为个人记录，不会被发送给 AI
- **Prompt 说明**：倾诉记录不出现在 prompt 中

## 实现细节

### 1. 倾诉记录状态判断

```typescript
const hasConfession = answers.confessionText?.trim();
const confessionAsStyleGuide = (answers.includeConfessionInImage ?? true) && hasConfession;  // 默认 true
const confessionAsContent = (answers.showConfessionInImage ?? false) && hasConfession;       // 默认 false
```

### 2. 事实清单更新

当倾诉记录作为内容时，添加到事实清单：

```typescript
const factSummary = [
  `主题场景：${answers.scene}`,
  `叙述者口吻：${narratorPart}`,
  `情绪关键词：${moodPart}`,
  hasUserTitle ? `标题文案：${title}` : null,
  hasUserDetails ? `用户补充的场景细节：${facts.map((f) => `${f.label}=${f.value}`).join("、")}` : null,
  confessionAsContent ? `用户的倾诉记录：${hasConfession}` : null,  // 仅当勾选"放在画面中"时添加
]
  .filter(Boolean)
  .join("；");
```

### 3. 文字约束条件动态调整

根据倾诉记录的用途，动态生成文字约束：

```typescript
const textConstraint = confessionAsStyleGuide
  ? `2) 所有可见文字（标题、贴纸、盖章、边注、票根、地图标注）可从白名单中选取，
       也可根据用户倾诉记录的关键词和情绪自由创作，但不要直接引用倾诉内容本身；`
  : `2) 所有可见文字（标题、贴纸、盖章、边注、票根、地图标注）只能从白名单中选取：
       [${whitelist.join(" / ")}]。若某处需要文字但白名单不足，改用图形、线条、
       抽象符号占位，绝不发明新词、新地名或假英文；`;
```

### 4. 倾诉记录风格指导短语

```typescript
const confessionStylePhrase = confessionAsStyleGuide
  ? `用户的倾诉记录（作为情绪和风格指导，可自由提取关键词用于文案）：
     「${hasConfession}」。请根据这段话的情绪、关键词和氛围来调整画面的整体风格和文案内容，
     但不要直接引用或写出这段话的原文。`
  : "";
```

### 5. 倾诉记录内容强制指令

```typescript
const confessionContentPhrase = confessionAsContent
  ? `【重要】用户的倾诉记录必须在画面中呈现：「${hasConfession}」。这段话是画面的核心内容，
     请将其以合适的形式（边注、贴纸、标签、手写感文字等）融入手帐拼贴中，
     确保观者能清晰看到这段倾诉。`
  : "";
```

## UI 变更

### 倾诉记录选项

在"今天的倾诉"部分添加了两个独立的勾选框：

1. **☑️ 将这段话作为风格指导和关键词提取，融入画面生成** （默认勾选）
2. **☐ 将这段话作为实际内容放在画面中** （默认不勾选）

用户可以：
- 都勾选：倾诉记录既作为风格指导，也作为实际内容显示
- 只勾选第一个：倾诉记录仅作为风格指导，不显示在画面中
- 只勾选第二个：倾诉记录作为实际内容显示，但文字受白名单限制
- 都不勾选：倾诉记录不影响生成

## 类型定义变更

### src/types.ts

```typescript
export type UserAnswers = {
  // ... 其他字段 ...
  
  /** 用户的倾诉记录：今天的感想、心情、发生的事情等 */
  confessionText?: string;
  
  /** 是否将倾诉记录作为风格指导和关键词提取，融入画面生成（默认 true） */
  includeConfessionInImage?: boolean;
  
  /** 是否将倾诉记录作为实际内容放在画面中（默认 false） */
  showConfessionInImage?: boolean;
};
```

## Prompt 示例

### 场景 1：都勾选

```
信息基础（仅能使用以下内容，其他视为未知）：
主题场景：一次旅程；叙述者口吻：像一本精致生活杂志；情绪关键词：怀旧、像电影；
用户的倾诉记录：今天天气很好，和朋友去了海边，虽然有点累但很开心。希望能把这份美好记录下来。

用户的倾诉记录（作为情绪和风格指导，可自由提取关键词用于文案）：
「今天天气很好，和朋友去了海边，虽然有点累但很开心。希望能把这份美好记录下来。」
请根据这段话的情绪、关键词和氛围来调整画面的整体风格和文案内容，
但不要直接引用或写出这段话的原文。

【重要】用户的倾诉记录必须在画面中呈现：「今天天气很好，和朋友去了海边，虽然有点累但很开心。希望能把这份美好记录下来。」
这段话是画面的核心内容，请将其以合适的形式（边注、贴纸、标签、手写感文字等）融入手帐拼贴中，
确保观者能清晰看到这段倾诉。

内容约束（必须严格遵守）：
...
2) 所有可见文字（标题、贴纸、盖章、边注、票根、地图标注）可从白名单中选取，
   也可根据用户倾诉记录的关键词和情绪自由创作，但不要直接引用倾诉内容本身；
...
```

**AI 可以做的事**：
- ✅ 根据"天气很好、海边、朋友、开心"等关键词创作文案
- ✅ 调整画面的整体氛围为温暖、明亮、欢快
- ✅ 在画面中显示倾诉内容
- ❌ 不直接写出原文，但可以改写或提取关键词

### 场景 2：只勾选"融入画面生成"

```
信息基础（仅能使用以下内容，其他视为未知）：
主题场景：一次旅程；叙述者口吻：像一本精致生活杂志；情绪关键词：怀旧、像电影

用户的倾诉记录（作为情绪和风格指导，可自由提取关键词用于文案）：
「今天天气很好，和朋友去了海边，虽然有点累但很开心。希望能把这份美好记录下来。」
请根据这段话的情绪、关键词和氛围来调整画面的整体风格和文案内容，
但不要直接引用或写出这段话的原文。

内容约束（必须严格遵守）：
...
2) 所有可见文字（标题、贴纸、盖章、边注、票根、地图标注）可从白名单中选取，
   也可根据用户倾诉记录的关键词和情绪自由创作，但不要直接引用倾诉内容本身；
...
```

**AI 可以做的事**：
- ✅ 根据倾诉内容的关键词创作文案
- ✅ 调整画面风格
- ❌ 不在画面中显示倾诉内容本身

### 场景 3：只勾选"放在画面中"

```
信息基础（仅能使用以下内容，其他视为未知）：
主题场景：一次旅程；叙述者口吻：像一本精致生活杂志；情绪关键词：怀旧、像电影；
用户的倾诉记录：今天天气很好，和朋友去了海边，虽然有点累但很开心。希望能把这份美好记录下来。

【重要】用户的倾诉记录必须在画面中呈现：「今天天气很好，和朋友去了海边，虽然有点累但很开心。希望能把这份美好记录下来。」
这段话是画面的核心内容，请将其以合适的形式（边注、贴纸、标签、手写感文字等）融入手帐拼贴中，
确保观者能清晰看到这段倾诉。

内容约束（必须严格遵守）：
...
2) 所有可见文字（标题、贴纸、盖章、边注、票根、地图标注）只能从白名单中选取：
   [一次旅程 / 像一本精致生活杂志 / 怀旧 / 像电影 / ...]。
   若某处需要文字但白名单不足，改用图形、线条、抽象符号占位，
   绝不发明新词、新地名或假英文；
...
```

**AI 可以做的事**：
- ✅ 将倾诉记录作为画面的实际内容显示
- ✅ 在画面中展示用户的倾诉文字
- ❌ 创作超出白名单的新文案

### 场景 4：都不勾选

```
信息基础（仅能使用以下内容，其他视为未知）：
主题场景：一次旅程；叙述者口吻：像一本精致生活杂志；情绪关键词：怀旧、像电影

内容约束（必须严格遵守）：
...
2) 所有可见文字（标题、贴纸、盖章、边注、票根、地图标注）只能从白名单中选取：
   [一次旅程 / 像一本精致生活杂志 / 怀旧 / 像电影 / ...]。
   若某处需要文字但白名单不足，改用图形、线条、抽象符号占位，
   绝不发明新词、新地名或假英文；
...
```

**AI 可以做的事**：
- ✅ 正常生成，不受倾诉记录影响
- ❌ 倾诉记录不出现在 prompt 中

## 文件修改

### src/types.ts
- 添加 `showConfessionInImage?: boolean` 字段

### src/App.tsx
- 修改默认值：`includeConfessionInImage: true`（默认勾选）
- 添加默认值：`showConfessionInImage: false`（默认不勾选）
- 添加第二个勾选框 UI

### src/lib/modelClient.ts
- 修改倾诉记录状态判断逻辑
- 添加 `confessionContentPhrase` 强制指令
- 更新 prompt 返回数组，包含两个倾诉相关的短语

## 测试场景

### 测试 1：都勾选
```
输入倾诉：今天心情很好，天气晴朗，和家人一起去公园散步。
勾选：✅ 融入画面生成 + ✅ 放在画面中
预期结果：
- 倾诉内容作为风格指导，AI 可自由创作文案
- 倾诉内容也必须显示在画面中
- 文案不受白名单限制
```

### 测试 2：只勾选"融入画面生成"
```
输入倾诉：今天心情很好，天气晴朗，和家人一起去公园散步。
勾选：✅ 融入画面生成 + ☐ 放在画面中
预期结果：
- 倾诉内容作为风格指导
- 倾诉内容不显示在画面中
- 文案不受白名单限制
```

### 测试 3：只勾选"放在画面中"
```
输入倾诉：今天心情很好，天气晴朗，和家人一起去公园散步。
勾选：☐ 融入画面生成 + ✅ 放在画面中
预期结果：
- 倾诉内容必须显示在画面中
- 文案受白名单限制
- 倾诉内容作为事实清单的一部分
```

### 测试 4：都不勾选
```
输入倾诉：今天心情很好，天气晴朗，和家人一起去公园散步。
勾选：☐ 融入画面生成 + ☐ 放在画面中
预期结果：
- 倾诉记录不影响 prompt
- 倾诉记录仅作为个人记录
- 画面生成正常进行
```

## 构建验证

✅ TypeScript 编译通过
✅ Vite 构建成功
✅ 无编译错误
✅ 无类型错误

## 总结

倾诉记录功能现在支持**两个独立的勾选项**，提供了更灵活的使用方式：

1. **融入画面生成**（默认勾选）：倾诉记录作为风格指导，去掉文字限制
2. **放在画面中**（默认不勾选）：倾诉记录作为实际内容，必须显示在画面中

用户可以根据自己的需求灵活组合，获得最个性化的生成结果。
