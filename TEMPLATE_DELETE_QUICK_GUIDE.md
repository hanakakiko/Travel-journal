# 模板删除功能 - 快速指南

## 新增功能

### 1. 模板列表中的删除按钮
在模板选择界面，每个模板卡片右侧现在有三个按钮：
- **查看** - 查看模板详情
- **使用** - 使用此模板开始创作
- **删除** - 删除此模板（新增）

### 2. 模板详情页面的删除按钮
在模板详情页面的底部 footer，现在有：
- **返回** - 返回模板列表
- **使用此模板** - 使用此模板
- **删除** - 删除此模板（新增，红色按钮）

## 使用方法

### 删除模板
1. 打开应用，点击"开始画手帐"
2. 在模板选择界面，找到要删除的模板
3. 点击模板卡片右侧的"删除"按钮
4. 确认删除（会弹出确认对话框）
5. 模板被删除

### 或者通过详情页面删除
1. 打开应用，点击"开始画手帐"
2. 在模板选择界面，点击"查看"进入模板详情
3. 在底部 footer 点击红色的"删除"按钮
4. 确认删除
5. 返回模板列表，模板已被删除

## 修复的 Bug

### 刷新后模板消失的问题
**已修复** ✅

现在刷新页面后，之前保存的模板会正确显示。

**原因**：修复了 `customTags` 的初始化逻辑，确保每次组件挂载时都能正确读取 localStorage 中的数据。

## 技术改进

### 1. 动态初始化 customTags
```typescript
// 之前：在模块加载时初始化（只执行一次）
customTags: getAllCustomTags()

// 之后：在组件挂载时初始化（每次挂载都执行）
const [answers, setAnswers] = useState<UserAnswers>(() => ({
  ...defaultAnswers,
  customTags: getAllCustomTags(),
}));
```

### 2. 添加 localStorage 变化监听
现在支持多标签页同步：
- 在标签页 A 删除模板
- 切换到标签页 B，模板列表自动更新

## 常见问题

### Q: 删除后能恢复吗？
A: 不能。删除是永久的，请谨慎操作。删除前会有确认对话框。

### Q: 为什么刷新后模板还在？
A: 这是正确的行为。模板保存在浏览器的 localStorage 中，刷新页面不会删除它们。

### Q: 多个浏览器标签页之间会同步吗？
A: 会的。如果在一个标签页删除了模板，其他标签页会自动更新。

### Q: 清除浏览器缓存会删除模板吗？
A: 是的。模板保存在 localStorage 中，清除浏览器缓存会删除所有数据。

## 相关代码位置

- 删除按钮 UI：[`src/App.tsx`](src/App.tsx) 第 1108-1160 行（模板列表）和 2455-2481 行（详情页面）
- 删除逻辑：[`src/lib/templateManager.ts`](src/lib/templateManager.ts) 第 58-67 行
- 状态管理：[`src/App.tsx`](src/App.tsx) 第 150-154 行（`handleDeleteTemplate`）
