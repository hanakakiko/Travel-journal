# 自定义标签编辑功能

## 功能概述

用户现在可以在表单中自定义添加和删除标签，同时系统会确保每个选项至少保留两个标签。这个功能适用于以下表单字段：

- **情绪** (mood) - 支持自定义添加新的情绪标签
- **氛围** (vibes) - 支持自定义添加新的氛围标签
- 其他多选标签字段（如排版形状、边缘风格、装饰元素等）

## 核心特性

### 1. 添加新标签
- 用户可以点击"添加"按钮在任何标签组中添加新的自定义标签
- 输入框支持回车键确认或点击确认按钮
- 支持按 Escape 键取消输入
- 自动去重，不允许添加重复的标签

### 2. 删除标签
- 自定义标签可以随时删除（显示 × 按钮）
- 默认标签只有在删除后仍有至少 2 个时才能删除
- 无法删除的标签显示 🔒 图标提示
- 删除标签时，如果该标签被选中，会自动从选中列表中移除

### 3. 标签持久化
- 所有自定义标签自动保存到浏览器 localStorage
- 刷新页面后自定义标签仍然存在
- 保存模板时，自定义标签也会被保存到模板中
- 应用模板时，模板中的自定义标签会被恢复

## 技术实现

### 新增文件

1. **`src/lib/tagManager.ts`** - 标签管理工具库
   - `addTag()` - 添加新标签
   - `removeTag()` - 删除标签（带验证）
   - `canRemoveTag()` - 检查是否可以删除标签
   - `isCustomTag()` - 检查是否是自定义标签

2. **`src/lib/customTagsStorage.ts`** - 自定义标签持久化工具
   - `getAllCustomTags()` - 获取所有自定义标签
   - `saveCustomTags()` - 保存自定义标签到 localStorage
   - `getCustomTagsForField()` - 获取某个字段的自定义标签
   - `addCustomTagToField()` - 添加自定义标签到某个字段
   - `removeCustomTagFromField()` - 从某个字段删除自定义标签

3. **`src/components/EditableTagGroup.tsx`** - 可编辑标签组件
   - 支持显示默认标签和自定义标签
   - 支持添加新标签
   - 支持删除自定义标签
   - 支持选择/取消选择标签

### 修改的文件

1. **`src/types.ts`**
   - 在 `UserAnswers` 类型中添加 `customTags?: Record<string, string[]>` 字段

2. **`src/App.tsx`**
   - 添加 `handleAddCustomTag()` 函数处理添加标签
   - 添加 `handleRemoveCustomTag()` 函数处理删除标签
   - 修改 `VisualFlavorPanel` 组件，使用 `EditableTagGroup` 替代氛围标签的静态列表
   - 修改 `InfoModal` 组件，传递新的标签编辑回调函数
   - 修改 `handleApplyTemplate()` 函数，恢复模板中的自定义标签

3. **`src/styles.css`**
   - 添加 `.editable-tag-group` 相关样式
   - 添加 `.tag-chip` 相关样式
   - 添加 `.tag-input-wrapper` 相关样式
   - 添加 `.tag-add-button` 相关样式

## 使用流程

### 添加新标签
1. 在表单中找到支持自定义标签的字段（如"情绪"）
2. 点击"添加"按钮
3. 在输入框中输入新标签名称
4. 按 Enter 或点击确认按钮
5. 新标签会立即出现在标签列表中

### 删除标签
1. 找到要删除的标签
2. 如果是自定义标签，点击标签右侧的 × 按钮
3. 如果是默认标签且可以删除（至少保留 2 个），点击标签右侧的 × 按钮
4. 如果标签无法删除，会显示 🔒 图标

### 选择标签
1. 点击标签即可选择/取消选择
2. 选中的标签会高亮显示
3. 选择的标签会被保存到表单数据中

## 数据存储

### localStorage 结构
```json
{
  "journal-custom-tags": {
    "mood": ["自定义情绪1", "自定义情绪2"],
    "vibes": ["自定义氛围1"],
    ...
  }
}
```

### UserAnswers 结构
```typescript
{
  mood: ["怀旧", "像电影", "自定义情绪1"],
  customTags: {
    mood: ["自定义情绪1"],
    vibes: ["自定义氛围1"]
  },
  ...
}
```

## 限制条件

1. **最少标签数** - 每个字段至少需要保留 2 个默认标签
2. **去重** - 不允许添加重复的标签
3. **自定义标签删除** - 自定义标签可以随时删除，不受最少数量限制
4. **默认标签删除** - 默认标签只有在删除后仍有至少 2 个时才能删除

## 未来改进方向

1. 支持场景细节字段的自定义标签编辑
2. 支持标签的编辑（修改标签名称）
3. 支持标签的排序
4. 支持标签的分类管理
5. 支持导出/导入自定义标签配置
