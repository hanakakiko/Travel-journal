# 增删画面功能实现

## 功能概述

将原来的"继续添加"按钮改为"增删画面"，点击后打开一个新的模态框，用户可以在其中：
1. **添加新图片** - 上传新的图片到画桌
2. **删除已上传的图片** - 管理和删除已经上传的图片

## 实现细节

### 1. 核心改动

#### App.tsx 中的状态管理
- 添加了 `isPhotoManagerOpen` 状态来控制图片管理模态框的打开/关闭
- 添加了 `deletePhoto(index)` 函数来删除指定索引的图片

#### 按钮行为变化
- **无图片时**：显示"选择照片"按钮，直接上传图片（原有行为）
- **有图片时**：显示"增删画面"按钮，点击打开图片管理模态框

#### 新增 PhotoManagerModal 组件
这是一个新的模态框组件，包含两个主要区域：

1. **添加新图片区域**
   - 支持拖拽上传
   - 支持多张图片同时上传
   - 显示上传进度

2. **已上传图片列表**
   - 网格布局展示所有已上传的图片
   - 每张图片显示：
     - 缩略图预览
     - 文件名
     - 文件大小
     - 云端上传状态（成功/失败）
   - 悬停时显示删除按钮
   - 点击删除按钮可以删除该图片

### 2. 删除功能实现

当用户删除图片时，会执行以下操作：
1. 从 `photos` 数组中移除该图片
2. 从 `remoteUrls` 数组中移除对应的远程链接
3. 从 `visionTags` 中清除该图片的 VLM 识别标签

这确保了数据的一致性和完整性。

### 3. 样式设计

新增的 CSS 样式包括：

- `.photo-manager-list` - 网格布局容器
- `.photo-manager-item` - 单个图片项
- `.photo-manager-preview` - 图片预览区域
- `.photo-manager-overlay` - 悬停时的覆盖层
- `.photo-delete-btn` - 删除按钮样式
- `.photo-manager-info` - 图片信息区域

响应式设计：
- 桌面端：每行显示多个图片（自适应）
- 移动端：调整网格列数和间距

## 文件修改清单

### 修改的文件

1. **src/App.tsx**
   - 导入 `Trash2` 图标
   - 添加 `isPhotoManagerOpen` 状态
   - 修改 `useEffect` 以支持新的模态框状态
   - 添加 `deletePhoto()` 函数
   - 修改上传按钮逻辑（条件渲染）
   - 添加 `PhotoManagerModal` 组件渲染
   - 新增 `PhotoManagerModal` 组件定义

2. **src/styles.css**
   - 添加图片管理器相关的 CSS 样式
   - 包括网格布局、悬停效果、删除按钮样式等

## 用户交互流程

### 初始状态（无图片）
```
用户看到 "选择照片" 按钮
↓
点击按钮或拖拽图片
↓
上传图片到画桌
```

### 有图片后
```
用户看到 "增删画面" 按钮
↓
点击按钮打开图片管理模态框
↓
用户可以：
  1. 添加新图片（上传区域）
  2. 删除已有图片（列表中的删除按钮）
↓
关闭模态框返回主界面
```

## 技术特点

1. **无缝集成** - 与现有的图片处理流程完全兼容
2. **数据一致性** - 删除图片时同时清理相关的所有数据
3. **用户友好** - 清晰的视觉反馈和直观的交互
4. **响应式设计** - 在各种屏幕尺寸上都能正常工作
5. **无障碍支持** - 包含适当的 ARIA 标签和语义化 HTML

## 测试建议

1. 测试上传图片后按钮文本变化
2. 测试删除图片功能
3. 测试删除后数据的一致性
4. 测试在模态框中添加新图片
5. 测试响应式布局
6. 测试删除后 VLM 标签的清理

## 兼容性

- 支持所有现代浏览器
- 支持移动设备
- 支持拖拽上传
- 支持多文件选择
